<%- include('navbar.ejs') %>


<div class="container mt-4">
  <h3>Edit Latest Job</h3>

  <form action="/admin/update_latest_job/<%= job[0].latest_jobs_id %>" method="POST">

    <div class="mb-3">
      <label class="form-label">Job Role</label>
      <input type="text" class="form-control" name="job_role"
             value="<%= job[0].job_role %>">
    </div>

    <div class="mb-3">
      <label class="form-label">Company Name</label>
      <input type="text" class="form-control" name="company_name"
             value="<%= job[0].company_name %>">
    </div>

    <div class="mb-3">
      <label class="form-label">Company Location</label>
      <input type="text" class="form-control" name="company_location"
             value="<%= job[0].company_location %>">
    </div>

    <div class="mb-3">
      <label class="form-label">Experience Required</label>
      <input type="text" class="form-control" name="emp_exp"
             value="<%= job[0].emp_exp %>">
    </div>
    <div class="mb-3">
      <label class="form-label">Job Description</label>
      <textarea class="form-control" name="job_desc"><%= job[0].job_desc %></textarea>

    <div class="mb-3">
      <label class="form-label">Languages Required</label>
      <input type="text" class="form-control" name="emp_lang"
             value="<%= job[0].emp_lang %>">
    </div>

    <div class="mb-3">
      <label class="form-label">Job Package</label>
      <input type="number" class="form-control" name="job_package"
             value="<%= job[0].job_package %>">
    </div>

    <button type="submit" class="btn btn-success">Update Job</button>
    
  </form>
</div>

<%- include('footer.ejs') %>
